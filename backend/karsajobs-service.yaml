apiVersion: v1
kind: Service
metadata:
  name: karsajobs
  namespace: karsajobs-ns
  labels:
    app: karsajobs
spec:
  selector:
    tier: app
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
      protocol: TCP
---

