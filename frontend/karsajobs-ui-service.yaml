apiVersion: v1
kind: Service
metadata:
  name: karsajobs-ui
  namespace: karsajobs-ns
  labels:
    app: karsajobs-ui
spec:
  selector:
    app: karsajobs-ui 
  type: NodePort  
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30000